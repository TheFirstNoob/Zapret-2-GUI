# Zapret 2 GUI

![GitHub Release](https://img.shields.io/github/v/release/TheFirstNoob/Zapret-2-GUI?include_prereleases&label=release)
![GitHub Downloads](https://img.shields.io/github/downloads/TheFirstNoob/Zapret-2-GUI/total?label=downloads)
![License](https://img.shields.io/badge/license-MIT-green)

GUI-обёртка для **Zapret 2** (winws2 / lua-desync) - обход фильтрации трафика у провайдера (DPI/ТСПУ) на Windows, без VPN.

**Текущая версия: Pre-Release 0.8** - сборки публикуются в разделе [Releases](https://github.com/TheFirstNoob/Zapret-2-GUI/releases).

> [!IMPORTANT]
> Программа сделана совместно с ИИ - я этого не скрываю. Делюсь рабочим результатом: он проверен на моей сети и у пользователей моего дискорда.
>
> На GitHub есть и другие инструменты с похожей задачей - какой выбирать, решаете только вы. Я делаю проект для себя и своих ребят, кто меня знает. Скачивайте там, где вам спокойнее.

## 📖 Содержание

- [🚀 Быстрый старт](#-быстрый-старт)
- [🎁 Какую версию скачать](#-какую-версию-скачать)
- [✨ Возможности](#-возможности)
- [Стратегии (пресеты)](#стратегии-пресеты)
  - [Как работает `auto` (автопереключение)](#как-работает-auto-автопереключение)
  - [Как собирается `custom` (личная стратегия)](#как-собирается-custom-личная-стратегия)
  - [Равные проценты: кого выбирает тестер?](#равные-проценты-кого-выбирает-тестер)
- [🔍 Прозрачность: что программа делает с системой и данными](#-прозрачность-что-программа-делает-с-системой-и-данными)
- [❓ Частые вопросы](#-частые-вопросы)
- [📖 Словарь](#-словарь)
- [📄 hosts.txt - запасной инструмент](#-hoststxt---запасной-инструмент)
  - [🌐 GitHub заблокирован/тормозит?](#-github-заблокировантормозит-актуально-с-082026)
  - [🌐 DNS - если сайты открываются "через раз"](#-dns---если-сайты-открываются-через-раз)
  - [🌐 YouTube: "нет интернета" внутри ютуба](#-youtube-открывается-но-пишет-нет-интернета-внутри-ютуба)
- [🦠 Антивирусы (важно)](#-антивирусы-важно)
- [🛠️ Сборка из исходников](#-сборка-из-исходников)
- [⚠️ Известные особенности](#-известные-особенности)
- [📁 Структура проекта](#-структура-проекта)
- [📄 Лицензия](#-лицензия)
- [Благодарности](#благодарности)
- [Справочные материалы](#справочные-материалы)

## 🚀 Быстрый старт

> [!WARNING]
> Не запускайте программу **из архива**, из папки **Загрузки/Документы** и не кладите EXE **прямо на рабочий стол** - программа предупредит и подскажет, куда её перенести (например `C:\Zapret2GUI\`). Файлы из браузера несут пометку "из интернета", из временной папки теряются данные, а на рабочем столе после первого обновления появится россыпь папок программы.

**Portable (рекомендуется):**
1. Распакуйте `Zapret2GUI-portable.zip` в путь без кириллицы (например `C:\Zapret2GUI\`)
2. Запустите `install.cmd` один раз - создаст ярлык "Zapret 2 GUI" на рабочем столе и в Пуск
3. Запустите обход через ярлык (права администратора запросятся автоматически)

**Классический EXE:**
1. Распакуйте `Zapret2GUI.zip`, запустите `Zapret2GUI.exe`, согласитесь с UAC
2. Нажмите **Запустить** (стратегия `default`)

**Lite:**
1. Распакуйте и запустите `start-default.bat` (простой запуск) - или `service.bat` для меню: выбор стратегии, запуск/остановка, служба

**Если что-то не работает** - откройте **"Проверка системы"** (диагностика: права, путь, расположение, служба, тип блокировки DNS/IP/SNI) или **"Подбор стратегии"** (тест всех стратегий с рекомендацией и топ-3).

**Требования:** Windows 10/11 x64, права администратора, WebView2 Runtime (встроен в Win10/11, для portable).

## 🎁 Какую версию скачать

| Архив | Что внутри | Для кого |
|---|---|---|
| **`Zapret2GUI-portable.zip`** ⭐ | Полный GUI на официальном Python (без сборщика, `pythonw.exe` подписан PSF) - **рекомендуется** | Все. Меньше всего ложных срабатываний |
| **`Zapret2GUI.zip`** | Классический GUI одним EXE (PyInstaller) | Кому удобен один файл |
| **`Zapret2GUI-lite.zip`** | Только winws2 + батники, без Python и GUI (как Zapret 1) | Минимализм / очень строгие антивирусы |

Во всех трёх архивах - один и тот же движок и набор стратегий (список и пояснения - в разделе [Стратегии](#стратегии-пресеты) ниже). После проверки программа соберёт личную стратегию `custom` - [как это работает](#как-собирается-custom-личная-стратегия).

**Подлинность:** качайте архивы **только** из раздела [Releases](https://github.com/TheFirstNoob/Zapret-2-GUI/releases) этого репозитория - рядом с каждым лежит файл `.sha256`. Сверить: `certutil -hashfile Zapret2GUI.zip SHA256` (или `Get-FileHash`) - строка должна совпасть с файлом `.sha256` **и** с хешем в описании релиза.

Как отличить оригинал от копий:

- Официальный репозиторий - только `TheFirstNoob/Zapret-2-GUI`. У аккаунта есть история и другие проекты.
- Свежий пустой аккаунт с одним архивом - не я.
- Честный форк на GitHub помечен "forked from …" - это нормально, так люди помогают.
- Тревожные признаки: код выложен отдельным проектом без ссылки на источник, либо похожий аккаунт-двойник.
- Ориентируйтесь на **аккаунт и репозиторий**, а не на название: похожих названий много, и само оно ничего не гарантирует. Сборки в форках и клонах - не мои, даже если название то же.

Официальный архив - обычный zip **без пароля** и без установщика. **Я никогда** не прошу отключать антивирус, платить или что-то "подтверждать". Если хеш не совпал - это не моя сборка: удалите и скачайте заново. Наткнулись на клон - отправьте репорт в GitHub: такие аккаунты уходят в бан.

### Не доверяете чужим EXE?

Понимаю. Два пути без наших сборок:

- **Lite** - самый простой вариант: те же батники, что в Zapret 1 (включить-выключить), без Python, служб и установщиков.
- **Собрать на официальной основе**: возьмите winws2 из сборки автора движка - [bol-van/zapret-win-bundle](https://github.com/bol-van/zapret-win-bundle) (там же cygwin и примеры запуска), скопируйте рядом наши `presets/` и `lists/`, а `lua/` и `blobs/` - из моего репозитория (они совпадают с апстримом), и запускайте своим `.cmd`. Пресеты - это файлы с аргументами winws2, пример запуска есть в самой сборке.

Это вариант для опытных: готовых кнопок в такой сборке нет (у бол-вана об этом честно написано).

## ✨ Возможности

| Что | Что это даёт |
|---|---|
| **Подбор стратегии** | Тестер перебирает пресеты, показывает результаты в живой таблице и даёт вердикт с топ-3 |
| **Личная стратегия `custom`** | Собирается после теста из лучших приёмов для Discord, Google и остальных сервисов |
| **Стратегия `auto`** | Сама переключает наборы правил, если связь начала сбоить |
| **Подбор блоба** | Перебор заготовок поддельных пакетов. Рабочая подставляется автоматически |
| **Перепроверка доменов** | Если домен не заработал на лучшей стратегии, программа проверит его с другими блобами |
| **Проверка системы** | Права, путь, служба, драйвер, тип блокировки (DNS / IP / SNI), отчёт для поддержки |
| **Анализ приложения/игры** | Живая картина сети процесса: куда ходит, где блок, что с UDP-пакетами |
| **Игровые блокировки** | Домены авторизации и UDP-фикс для конфликтных игр (Wardogs уже добавлен) |
| **Спорные домены** | Домены, которые у части провайдеров под обходом ломаются: живая проба и тумблер "в обход" |
| **ASN-скан** | Картина фильтрации у вашего провайдера: 110 IP-проб с "белым" SNI |
| **CDN-стабилизатор** | Лечит сайты, которым мешает DPI, следящий за соединением (медиа, загрузки, видео) |
| **Служба Windows** | Автозапуск обхода после перезагрузки и самовосстановление при падении |
| **Списки** | Разделы "в обход" и "не трогать" для доменов и IP-сетей. Правки применяются ко всем стратегиям |
| **Переключатели** | Игровые порты, Discord Voice, Авто-список, общий IP-обход, DEBUG-лог |
| **Обновление из окна** | GitHub → зеркало, с автоматическим бэкапом настроек и списков |
| **Резервная копия** | Экспорт/импорт конфига и списков одним файлом (кнопки в подвале меню) |
| **Починить драйвер** | Самолечение службы WinDivert. Честно скажет, если нужна перезагрузка |
| **Переход с Zapret 1** | Найдёт старую службу и предложит убрать её в один клик |
| **Zapret 1 (запасной)** | Запуск классических стратегий из того же окна |
| **Журналы** | `ui_debug.log` и `test_session.log` пишутся локально и помогают разобрать непонятную ситуацию |
| **Быстрый старт** | Карточка из 3 шагов для новичка: запустить → проверить → подобрать стратегию |

## Стратегии (пресеты)

Названия приёмов технические (те же имена и у пресетов): **fake** - отправка поддельного пакета, **split** - разрыв запроса на части, **disorder** - части уходят в обратном порядке, **multisplit** - многократный разрыв. Понимать их не обязательно - тестер сам подскажет подходящий вариант.

| Пресет | Подход |
|---|---|
| `default` | **Универсальная** - fake (drop + repeats) + multisplit |
| `default-alt` | То же, но discord-фейки с **`tls_mod=rnd,dupsid`** (случайные поля + session_id из реального hello) - DPI не может зафиксировать "всегда одинаковый фейк". Попробуйте, если `default` на Discord работает нестабильно |
| `auto` | То же, но с **автопереключением стратегий** при неудачах (перебор по кругу). Попробуйте, если `default` не справляется на вашей сети |
| `tcpmd5-fake` | Fake + `tcp_md5` - для сетей, где фильтр вычисляет поддельные пакеты по особенностям TCP-соединения |
| `fake-only` | Только fake |
| `fakedsplit` | Fake + fakedsplit |
| `fake-disorder` | Fake + disorder (кастомная lua) |
| `fake-multidisorder` | Fake + multidisorder |
| `hostfakesplit` | Подмена хоста + разрыв |
| `multisplit-pure` / `multisplit-seqovl` | Чистые разрывы |

### Как работает `auto` (автопереключение)

`auto` - та же логика, что `default`, но с **самовосстановлением**: внутри каждой части зашиты три набора правил обработки (полный `fake + multisplit`, только `fake`, только `multisplit`), а переключением между ними управляет механизм `circular` (по кругу).

- Он следит за соединением: если видит **3 неудачи** (сервер резко сбросил соединение - RST - или один и тот же пакет передаётся повторно) - переключает набор правил на следующий и продолжает по кругу.
- Если соединение после переключения ожило - счётчик сбрасывается, и стратегия возвращается к основному набору.
- Если неудач не было **дольше 60 секунд** - счётчик тоже сбрасывается (старые сбои не накапливаются).

**Когда использовать:** если `default` на вашей сети периодически "отваливается" - `auto` сам подстроится. Цена - чуть больше служебного трафика (обход дополнительно следит за входящими ответами, чтобы заметить сбой). Если `default` стабилен - `auto` не нужен.

### Как собирается `custom` (личная стратегия)

После прогона тестера программа строит **персональную стратегию под вашу сеть**:

1. Каждый домен из теста привязывается к группе сервисов: **Discord**, **Google** и **General** (все остальные сайты, например GitHub).
2. Для каждой группы считается, насколько хорошо сработала **каждая** стратегия, и в сборку идут лучшие правила:
   - для Discord - у стратегии с лучшим результатом по Discord.
   - для Google - у лучшей по Google.
   - для остальных - у лучшей по General.
3. `auto` в сборке не участвует: его механика (`circular`-перебор) - отдельный режим со своей lua-инициализацией, а не часть готовой стратегии. `custom` - это только комбинация честных правил.
4. Непроверяемые тестером части (голос, медиа-порты, QUIC) всегда берутся из `default` - там они проверены.
5. Собранный `custom` **проверяется движком** (пробный запуск, затем настоящий): если не запустился - в вердикте честно отобразится ошибка.

`custom` пересобирается **каждым** прогоном теста - старая версия в нём не участвует, так что стратегия всегда свежая. Если разные стратегии лучше в своих группах - `custom` соберёт их вместе, и это может работать лучше, чем любая одна стратегия.

### Равные проценты: кого выбирает тестер?

Стратегии всегда идут в одном и том же порядке: `default` тестируется первым, `auto` и `custom` - последними. При равных результатах рекомендуется **первая протестированная** (обычно `default`) - и вердикт прямо это объясняет: стратегии равнозначны (пресеты под разные сети/провайдеры), а если одна начала сбоить - переключайтесь на другую из списка. Погрешность прогона учитывается: разница в **±1 домен** от лучшей считается практически равной (даже один случайный сбой двигает рейтинг на целый домен).

## 🔍 Прозрачность: что программа делает с системой и данными

Ничего не скрываем - вот полный список того, что программа делает:

**Система (только необходимое для обхода):**
- при запуске обхода загружается драйвер **WinDivert** (идёт в комплекте, open-source) - он перехватывает и модифицирует сетевые пакеты на вашем ПК. Служба драйвера создаётся автоматически.
- **служба автозапуска** ставится только по вашему нажатию ("Установить службу").
- для части стратегий включается системная настройка **TCP timestamps** - этого требует движок (состояние видно в "Проверке системы"). Вернуть как было можно командой:

  ```cmd
  netsh int tcp set global timestamps=disabled
  ```
- **hosts-файл программа НЕ трогает** - приложенный `hosts.txt` вы подключаете сами, если он нужен.
- все свои файлы программа создаёт только в своей папке.

**Данные:**
- **телеметрии нет**: никакая статистика, идентификаторы и личные данные никуда не отправляются.
- журналы (`ui_debug.log`, `test_session.log`) пишутся локально, в папке программы.
- отчёты для поддержки создаются локально и передаются, только если вы сами их отправите.
- галочка согласия в тестере - для случаев, когда вы добровольно делитесь отчётом.

**Сеть:**
- проверка обновлений - запрос к GitHub/зеркалу. Обновление ставится только по вашему нажатию.
- диагностика/тестер/сканы делают сетевые пробы к публичным сайтам (по ним и определяется блокировка).
- больше никаких обращений "наружу" нет.

**Чужие компоненты:** движок `winws2` + lua-скрипты - [zapret2](https://github.com/bol-van/zapret2), драйвер - [WinDivert](https://github.com/basil00/WinDivert). Полный список - в [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).

## ❓ Частые вопросы

**Обход не запускается / кнопка не работает**

Проверьте по очереди:
- путь без кириллицы (`C:\Zapret2GUI\`)
- права администратора
- нет других запущенных обходов (WinDivert конфликтует)
- программа запущена не из архива и не из "Загрузок"

Точку отказа покажет "Проверка системы".

**Ничего не пробивается (0 % на всех стратегиях)**
- Запустите "Подбор стратегии" - тестер переберёт пресеты и соберёт `custom`.
- Если не помогло - "Проверка системы" покажет тип блокировки (DNS/IP/SNI).
- При IP-блоке поможет общий IP-обход или hosts/WARP.

**YouTube/Google "красные" в тестере, но в браузере работают**

Это нормально: TCP-проба "чужим клиентом" (curl) сквозь обход не показательна - браузер ходит через QUIC. Такие хосты помечаются "≈ QUIC" и не считаются провалом. Проверяйте реальный опыт в браузере.

**Discord работает, но голос/звонки не идут**
- Попробуйте переключатель Discord Voice → "UDP-длина".
- Если не помогло - смените голосовой регион сервера в клиенте Discord.

**Discord не открывается, а остальное работает**
- Включите "Альт-режим Discord" в "Экспертных инструментах" и перезапустите обход (если стоит служба - переустановите её кнопкой).
- Альт-режим касается **только Discord**: остальные сервисы работают и без него.
- Если и на нём Discord иногда капризничает - это постоянная "гонка" с фильтрами провайдера, а не поломка программы: попробуйте другую стратегию или обновление позже.

**Игра не подключается к серверу (выкидывает в меню), хотя авторизация работает**
- Откройте "Игровые блокировки" (экспертные инструменты) и включите для игры домены (авторизация/лобби) и UDP-фикс (подключение к серверам). Wardogs уже добавлен и включён.
- Домены применяются сразу к новым подключениям, UDP-фикс - после перезапуска обхода (кнопка "Переустановить службу" в шапке вкладки).
- Если игра всё равно не заходит - нажмите "Анализ приложения" в её карточке: увидите, куда игра стучится, и сможете дополнить диапазон/порт в её записи.

**Защитник ругается на программу**

Программа безопасна: она ничего не ломает и не крадёт данные. Защитник реагирует не на вирус, а на драйвер перехвата трафика и установку службы - на это же иногда ругается и на оригинальный zapret.

Чаще всего срабатывает облачная проверка свежескачанного файла, то есть Защитник ругается не на все версии. Если предупреждение появилось:
- в уведомлении выберите "Разрешить" (в журнале защиты - "Разрешить на устройстве" или "Восстановить").
- в исключения вносить не обязательно: исключение - это полное доверие к файлу, решайте сами (подробнее - в разделе "Антивирусы").

**GitHub скачивается медленно/не скачивается**
После 0.6 GitHub лечится самим обходом. Если всё ещё нет - см. раздел про hosts/WARP ниже.

## 📖 Словарь

Короткие пояснения к словам, которые встречаются в тексте и в программе:

- **Обход** - программа вмешивается в запрос так, чтобы фильтр провайдера не распознал сайт и пропустил соединение.
- **DPI** - система анализа и фильтрации трафика у провайдера. Государственная версия называется **ТСПУ**.
- **Пресет (стратегия)** - готовый набор правил обхода (`default`, `auto` и другие).
- **Фейк (fake)** - поддельный пакет, который обход отправляет, чтобы сбить фильтр с толку.
- **Сплит (split), disorder, multisplit** - приёмы обхода: запрос режется на части и отправляется в обычном или перемешанном порядке.
- **Блоб** - заготовка поддельного пакета. "Блоб фейка" подбирается кнопкой в дополнительных параметрах.
- **Голый тест (naked)** - проверка без обхода: показывает, как сеть работает "в голую".
- **SNI** - имя сайта в начале соединения. По нему фильтр понимает, куда вы идёте.
- **QUIC** - современный протокол, который браузеры используют для видео и части сайтов.
- **DoH** - DNS-запросы внутри браузера (шифрованные). Из-за него видео YouTube может идти на недоступные серверы.
- **WARP** - бесплатный сервис Cloudflare, используется как запасной обход.
- **ipset / IP-обход** - обход не по доменам, а по IP-сетям.
- **Журналы (логи)** - локальные файлы с записями о работе программы: `ui_debug.log`, `test_session.log`.

## 📄 hosts.txt - запасной инструмент

С 0.6 основные блоки (Discord, GitHub, YouTube) обходятся самим движком. Но есть вещи, которые движок не берёт - для них приложен готовый [`hosts.txt`](hosts.txt) (469 строк, синхронизирован с репозиторием Zapret 1 от Flowseal + мои дополнения):

**Что внутри:**
- **Discord-голос: Finland-серверы** (`finland10000-10019.discord.media`) - пиннинг на рабочий IP. Если голосовые каналы не открываются/обрываются - это первый кандидат (взято из Zapret 1, где проверено)
- **ИИ-сервисы через антиблок-работник** - ChatGPT, Gemini/AI Studio, Claude, xAI, Spotify, Deepl и др.: гео-обход, когда сервис отвечает "регион не поддерживается"
- **GitHub-fix** - githubusercontent/raw/objects/avatars через Fastly-эджи (если обход не справился на вашей сети)
- **Антитрекер-заглушки** - мусорные домены на `0.0.0.0`

**Как применять:**
1. Откройте `C:\Windows\System32\drivers\etc\hosts` (от администратора)
2. **Дописывайте строки из hosts.txt в конец файла** - **никогда** не заменяйте файл целиком (полная замена может лишить службу DNS прав на чтение, и Windows будет молча игнорировать hosts)
3. `ipconfig /flushdns`

**Важно:** записи hosts не мешают другим программам и сайтам - это точечные пиннинги конкретных доменов, остальной трафик не затрагивается. Если какой-то IP "умер" (сайт перестал открываться, а без hosts - открывается) - удалите его строки или замените на запасные IP (списки в комментариях файла).

### 🌐 GitHub заблокирован/тормозит? (актуально с 08.2026)

После 0.6 github.com и сопутствующие домены лечатся самим обходом. Если на вашей сети всё ещё режется - запасные пути:

1. **hosts-пиннинг** (раздел выше) - блок github-fix из hosts.txt
2. **WARP** (Cloudflare) - обходит полностью
3. **jsDelivr-зеркало** для скачивания релизов:
   `https://cdn.jsdelivr.net/gh/TheFirstNoob/Zapret-2-GUI@main/Windows%20build/Zapret2GUI.zip`
   (для lite/portable - то же имя с суффиксом)

### 🌐 DNS - если сайты открываются "через раз"

Иногда помогает смена системного DNS (заблокированный DNS у провайдера отдаёт кривые ответы):

| Провайдер | Основной | Альтернативный |
|---|---|---|
| **Google Public DNS** | `8.8.8.8` | `8.8.4.4` |
| **Cloudflare** | `1.1.1.1` | `1.0.0.1` |
| **Quad9** (блокирует вредоносные) | `9.9.9.9` | `149.112.112.112` |

Меняется в: *Параметры → Сеть и Интернет → Wi-Fi/Пропускная способность → Свойства адаптера → IP-назначение → DNS-сервер → Вручную*.

### 🌐 YouTube открывается, но пишет "нет интернета" (внутри ютуба)

Страница грузится (обход работает), но видео не идёт - плеер ходит на медиа-хосты `*.googlevideo.com` через **DNS-резолвер браузера (DoH)**, и резолвер может отдавать недоступные кэш-серверы. Лечится заменой DNS-провайдера **внутри браузера**:

| Браузер | Где менять |
|---|---|
| Opera / Opera GX | Настройки → Конфиденциальность → Use DNS over HTTPS → сменить провайдера (или выключить) |
| Chrome / Edge | Настройки → Конфиденциальность → Безопасность → Use secure DNS → сменить/выключить |
| Firefox | Настройки → Приватность → DNS over HTTPS → сменить/выключить |

Попробовать по очереди: **Quad9 (9.9.9.9)** → **Cloudflare** → выключить DoH (будет системный). Актуально для всех браузеров с встроенным DoH - резолвер влияет на то, к какому кэш-серверу YouTube направит видео. С системным DNS работает та же логика (см. таблицу выше).

**Бонус-советы с форума Zapret 1 (Ростелеком):**
- YouTube "тормозит/через раз" → **отключите QUIC в браузере**
  (chrome://flags → Experimental QUIC protocol → Disabled)
- Смена DNS на **9.9.9.9 / 149.112.112.112** заметно улучшает загрузку
  видео на РТК

## 🦠 Антивирусы (важно)

WinDivert - инструмент перехвата трафика, на котором работает zapret. Он встречается и в хороших, и в плохих программах, поэтому часть антивирусов относит его к RiskTool/PUA. Вирусом он не является.

**Ложные срабатывания - обычное дело для таких инструментов.** Защитник Windows иногда помечает даже официальные архивы zapret от bol-van (детект вида `Trojan:Win32/…!cl`), а иногда - мои сборки: только за драйвер перехвата и установку службы. Это поведенческая эвристика, а не заражение.

**Что делать, если Защитник пометил файл:** в уведомлении выберите **"Разрешить"** (в журнале защиты - "Разрешить на устройстве" или "Восстановить"). Этого достаточно, программа продолжит работать.

**Исключения - на ваше усмотрение.** Исключение означает "доверять этому файлу/папке полностью" - если добавляете, делайте это осознанно. Я не прошу вносить в исключения.

**Меньше всего ложных реакций** - у `portable` (запускается официальный Python, без самосборного EXE) и `lite` (только .bat и бинарники движка). Служба ставится напрямую - `winws2.exe` бинарником, как в Zapret 1, без cmd-обёрток, на которые поведенческие детекты реагируют чаще.

## 🛠️ Сборка из исходников

```bat
git clone https://github.com/TheFirstNoob/Zapret-2-GUI.git
cd Zapret-2-GUI
python main.py                  :: запуск из исходников
python build.py                 :: классический EXE  -> dist\Zapret2GUI.exe
python build_portable.py        :: portable (без сборщика) -> Windows build\Zapret2GUI-portable.zip
python build_lite.py            :: lite на .bat     -> Windows build\Zapret2GUI-lite.zip
```

## ⚠️ Известные особенности

- **Кириллица в пути** - не запускается без коротких имён 8.3. Держите путь ASCII (`C:\Zapret2GUI\`)
- **Загрузки/Документы/архив/рабочий стол** - программа предупредит: данные во временной папке теряются, файлы из браузера проверяются агрессивнее, папки могут синхронизироваться. Держите программу в отдельной папке (`C:\Zapret2GUI\`)
- **Killer NIC** - конфликтует с WinDivert. Отключите Bandwidth Control
- **Яндекс.Браузер** принудительно подменяет DNS - тестируйте в Firefox/Chrome
- **Одновременно два обхода** - нельзя, они мешают друг другу (оба перехватывают пакеты через WinDivert). Примеры: Zapret 2 GUI + Zapret 1, Zapret + GoodbyeDPI, две копии этой программы.

## 📁 Структура проекта

```
├── main.py               # точка входа (UAC, webview GUI)
├── server/server.py      # локальный HTTP backend
├── core/                 # логика: launcher, tester, diagnostics, служба
├── frontend/             # SPA: HTML/CSS/JS
├── presets/ lua/ blobs/ lists/ bin/ windivert/
├── build.py              # сборка EXE (PyInstaller)
├── build_portable.py     # сборка portable (официальный Python, без паковщика)
├── build_lite.py         # сборка lite (.bat без Python)
└── AI_DOCS/              # техническая документация для агентов ИИ
```

## 📄 Лицензия

Собственный код проекта (GUI, backend, сборка) - [MIT](LICENSE): берите и используйте как угодно, в том числе в своих проектах.
Движок обхода, драйвер и часть данных - сторонние компоненты под своими лицензиями:
полный список и тексты - [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) (zapret2/bol-van - MIT, WinDivert - LGPLv3/GPLv2 и др.).

## Благодарности

- [Запрет 2 (zapret2)](https://github.com/bol-van/zapret2) - bol-van, оригинальный проект (движок winws2 и lua)
- [Flowseal/zapret-discord-youtube](https://github.com/Flowseal/zapret-discord-youtube) - Zapret 1, по сути то, чем пользуется большинство (структура батников, данные, сравнение в тестах)
- [V3nilla/IPSets-For-Bypass-in-Russia](https://github.com/V3nilla/IPSets-For-Bypass-in-Russia) - CIDR-списки для ipset-обхода
- [DanielLavrushin/b4](https://github.com/DanielLavrushin/b4) - примеры нетипичных механизмов DPI-обхода (алгоритмы и параметры fake)
- [WinDivert](https://github.com/basil00/WinDivert) - Graham Cleus
- Сообществу Windows-порта winws и движка winws2 (lua-desync)

## Справочные материалы

- [tspu-docs](https://github.com/DanielLavrushin/tspu-docs) - документация по
  архитектуре и настройке ТСПУ (DPI-движок EcoFilter, двухстадийная блокировка,
  деградация протоколов, обфускация). Полезно для понимания, как работают
  блокировки и какие механизмы обхода уязвимы.
