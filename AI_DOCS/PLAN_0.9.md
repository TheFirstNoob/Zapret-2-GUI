# План 0.9 — состояние и что осталось (живой документ)

> Обновлено 2026-09-24. Читать после сжатия сессии: сначала AGENTS.md
> (снапшот), затем этот план и `Zapret2GUI — Pre-Release 0.9.md` на рабочем
> столе. Все коммиты — в main.

## СДЕЛАНО (кратко, чтобы не переделывать)

- **Безопасность**: подписанные обновления (Ed25519, PUBKEY `f6a4a0…72e`,
  отпечаток `50DCECF29451`), fail-closed апдейтер, офлайн-ключ + инструменты
  (`tools/security/`), чеклист выпуска `AI_DOCS/RELEASE_CHECKLIST.md`.
- **Discord**: база переведена в drop-режим (доказана утечка полного hello),
  повторы только на fake; ALT-режим (nodrop) — тумблер в экспертных + галочка
  в тестере.
- **Wardogs**: рецепт найден и в продукте — вкладка «Игровые блокировки»
  (домены авторизации + UDP-фикс `fake:...:payload=all`, порт 4192 и
  диапазоны 54.115/54.228/54.216/3.218). Баг-корень: lua fake/udplen по
  умолчанию не трогают unknown-UDP (нужен `payload=all`), GameFilter=udp
  в лаунчере был из-за этого холостым — исправлен.
- **Сборка**: `Zapret2GUI.spec` (впервые в гите): исключены
  cryptography/bcrypt, отфильтрованы корневые дубли cygwin1/WinDivert и
  Python.Runtime.xml; версия exe генерируется из `core.config.VERSION`.
  Итог: exe 18.5 → **13.7 МБ**, zip 18.3 → **13.5 МБ**.
- **WinDivert**: разобраны ложные тревоги диагностики (путь с пробелами;
  Start=4/DeleteFlag=1 — НОРМА WinDivert). Починен реальный хвост
  «помечена на удаление» (остановка обхода → выгрузка драйвера → пересоздание).
- **Тестер**: строки больше не зависают в «тестируется…» (сервер отдаёт
  `completed`, фронт закрывает по одному разу); «голый тест» закрывается при
  переходе; «Хосты» = `зачётные/зачётные (всего)`, подсказка на заголовке.
- **Навигация/главная**: 3 основных пункта + сворачиваемая группа
  «Экспертные инструменты» с категориями; «Параметры запуска» Zapret 2 —
  РАЗВЁРНУТЫ по умолчанию (решение юзера), «Zapret 1» — свёрнут.
  «Проверить работу» реально запускает диагностику.
- **Доки**: правила (первое лицо «я» в пользовательских текстах),
  README (признаки подделок, каналы), ченджлог 0.9, чистка мусора и
  .gitignore (backups/updates/games/etl…).
- **README — стиль по отзыву друзей**: жаргон → понятные слова, обычные
  кавычки вместо «ёлочек», без `;` и длинных тире, интро сокращено,
  «Быстрый старт» переехал наверх, «Возможности» — таблицей, FAQ — списками,
  добавлен «Словарь» (13 терминов), починены b4/защитник/два обхода.
  Правила стиля — в `rules.md` (в UI пунктуацию НЕ менять оптом).

## ОСТАЛОСЬ ПО 0.9

### Батч 3 — тестер (UX-ревью) ✅ сделано (9035222, 67bb455)
- [x] «Расширенный режим (Zapret 1 + анализ)» спрятан в «Дополнительно»
  (со стилем details: сворачивается, не выглядит равным основному).
- [x] Время: «обычно 2–4 минуты» + «после теста защита вернётся сама (в том
  состоянии, что было до теста)»; восстановление защиты подтверждено кодом
  (`_restore_protection_after_naked` + тост на `final.restored`).
- [x] «Хосты» = зачётные + всего проверено в скобках + подсказка на заголовке.

### Батч 4 — свои подсказки/поповеры (большая правка ревью)
- [ ] Компонент popover (hover+focus+Esc, заголовок+1–3 строки) вместо
  штатных `title` для длинных текстов: селект блоба, «Хосты», бейджи CDN,
  техдетали диагностики, режимы Discord Voice.
- [ ] Подвал меню: 3 иконки (экспорт/импорт/инфо) — либо подписи, либо меню
  из подписанных пунктов; убрать дублирование с «Резервной копией» в Списках.

### Батч 5 — списки/спорные/игры — ✅ почти всё (ce45a1f, 0be160d)
- [x] Списки: «в обход / не трогать»; IP-редакторы свёрнуты (details);
  «Здоровье списков» под редакторами; старый блок экспорт/импорт убран
  (id `settingsImportFile` переехал в подвал меню; `btnSettingsExport`
  остался лишь как необязательный хук — код с nullable-guard).
- [x] Спорные: шаг «3. Перезапуск обхода» убран, добавлена подпись
  «изменения применяются сразу»; тумблеры подписаны «в обход».
- [x] Игры: кнопка «Сохранить» убрана (автосохранение + пояснение);
  «Переустановить службу» в шапке (= MainPage.svcInstall с confirm);
  в карточке «Анализ приложения» (поле `process`, Wardogs → WardogsClient.exe,
  бэкфилл в load_games; прыжок на #probe + предзаполнение + старт анализа).
- [ ] Осталось из игровой темы: кнопка «Проверить домены» (проба доменов
  прямо из карточки; нужен API пробы домена или переиспользование
  probe-эндпоинта) и «из probe в игру» (добавление найденных доменов).

### Батч 6 — CDN/ASN/онбординг
- [ ] CDN: строка под кнопкой «на время скана обход выключится и вернётся сам».
- [ ] Диагностика: сценарии «YouTube открывается, видео не идёт» (DNS/DoH) и
  «голос Discord не идёт» — в подсказки/«Помощь».
- [ ] Постоянный вход «Помощь» (сценарии: не работает, перенос настроек,
  обновления) + развести тур и «О программе» (сейчас исчезают навсегда).

### Релиз 0.9 (по AI_DOCS/RELEASE_CHECKLIST.md)
- [ ] Батчи выше → обновлять ченджлог по мере готовности.
- [ ] VERSION → «Pre-Release 0.9» (config; manifest'ы portable/lite и версия
  exe подхватятся автоматически).
- [ ] Пересобрать 3 дистрибутива (exe теперь через spec — версия и фильтры
  уже внутри), прогнать тесты безопасности, СМОУК нового exe (служба,
  обновление, тестер — базово проверены юзером на промежуточной сборке).
- [ ] Подписать релиз `tools/security/sign_release.py`, прикрепить ассеты
  (канонические имена!), в описании релиза — SHA256.

### Задачи на 0.9 (обновлено 2026-09-24)
- [ ] **Обновить winws2 1.0.5 → 1.0.5.2** — в апстриме серьёзная
  оптимизация производительности. Шаги: сверить changelog апстрима, заменить
  `bin/winws2.exe` (и lua, если менялись), проверить пресеты (dry-run +
  реальный запуск), прогнать тестер на своей сети, пересобрать дистрибутивы.
  VERSION до релиза НЕ бампить.
- [ ] Ревизия release-набора пресетов — по желанию, после winws2.

## ЗАКРЫТО/СНЯТО (не переделывать)
- Холодный старт Discord — не ловим: не исключено, что флак со стороны
  фильтров (новость про РКН); автор оставил наблюдение у себя.
- Переустановка службы — за автором (сделает сам, когда решит).
- Мегафон-кейс — закрыт (решено).
- ipset-база — актуальна, новых наборов нет (diff с V3nilla не нужен).
- Telegram mtproto A/B — без сторонней прокси-программы невозможно,
  интеграция не планируется.
- YouTube `--wf-udp-in=443`+hostlist-auto — снято с активных задач,
  перенесено в «Запасные планы» ниже (обычный обход QUIC стабилен).
- Папка Flowseal — файлы внутри старого Zapret 1: можно делать что угодно
  (рабочая копия, версия неактуальна).
- `tools/_ui_shots.py` — справка: headless Edge только через PowerShell
  `Start-Process` (иначе «прокидывается» в живой Edge и выходит).

## ЗАПАСНЫЕ ПЛАНЫ (если появится нетипичный блок)

- **YouTube/QUIC, если однажды пробьёт по-новому.** Обычный обход
  (fake + блоб) стабилен: вмешиваться в QUIC почти не можем, а разные блоки
  у разных провайдеров уже учтены. Если появится нетипичный блок видео:
  1) вручную добавить 2–3 ключевых поддомена googlevideo в «в обход»
     (либо всю поддоменную сеть — почти штатно и так покрыта);
  2) только как эксперимент — `--wf-udp-in=443` + авто-список, чтобы
     winws2 видел входящий QUIC и заносил медиа-домены сам.
     Минус: список может сильно раздуться и постоянно дёргать авто-список,
     поэтому по умолчанию не включаем.

## С ЧЕГО НАЧАТЬ ПОСЛЕ СЖАТИЯ
1. Прочитать AGENTS.md (снапшот) + этот файл + ченджлог 0.9 на рабочем столе.
2. Батч 4 (поповеры + подвал меню) — аккуратно и точечно, с проверкой
   скриншотом (в UI пунктуацию/тексты оптом не менять — rules.md).
3. Батч 6 (CDN/диагностика/«Помощь»).
4. winws2 1.0.5 → 1.0.5.2 (сверить апстрим, заменить, проверить пресеты).
5. Остаток батча 5 (проба доменов из карточки игры, «из probe в игру») —
   можно после релиза.
6. Релиз 0.9 по `AI_DOCS/RELEASE_CHECKLIST.md` (VERSION бампить только
   в момент релиза — не раньше).
