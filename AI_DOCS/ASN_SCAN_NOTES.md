# ASN-скан и ландшафт DPI: хроника, методология, находки (2026-09-06)

> **Файл непрерывности.** Если сессия прервалась (кончились токены, смена
> IDE) — здесь всё: что делали, что выяснили, что внедрили, что НЕ
> выяснили, и что делать дальше. Читается вместе со STRATEGY_ROADMAP.md и
> STRATEGY_TRIALS.md.

## 1. Контекст: зачем появился ASN-скан

Пользователь показал чужой инструмент (Runnin4ik/dpi-detector 4.2.4,
Python/httpx; исходники разобраны — см. §5): таблица ~110 IP-проб по ASN
(Hetzner/DO/OVH/Vultr/CDN77/CF/AWS/Akamai/Oracle/Gcore/Scaleway/Contabo/
Clouvider/FranTech/M247/Zenlayer/IOMART/Melbicom и др.) со статусами
OK / DETECTED / TCP RST / TLS RST / SYN DROP / TIMEOUT и DNS-таблицей
(nxdomain-спуфинг по ~40 UDP/DoH/DoT серверам).

Задача: повторить в нашем CDN-скане (вкладка «CDN-стабилизация»).

## 2. Хроника методологии (3 итерации — важно не откатить!)

1. **v1 (ошибка):** пробы шли ПОД работающим десинком пользователя.
   При включённом ipset-режиме пробные IP лежат в ipset-all → наш
   fake+multisplit применялся к белым SNI-пробам → результаты были смесью
   «ТСПУ + наш обман». Диагностировано сравнением: SYN DROP-набор из
   ASN-скана == «ломает»-набор из CDN A/B (DO/VLT/OVH/CV/CDN77/HE-01☆…).
2. **v2 (фикс, текущая):** скан сам глушит защиту (`_run_asn_scan`
   в server/server.py: kill + `asn_scan()` + сервис-осведомлённый
   `_restore_protection_after_naked`), конкурентность 4 (пачки SYN в один
   ASN будят антискан хостеров), curl с `-S` (текст ошибок не глушится),
   exit 28 классифицируется по stderr (CONNECT-фаза = SYN DROP; общий
   таймаут = TIMEOUT), «не отвечает» подтверждается повтором через 1.2 с
   (мобильная сеть теряет SYN рывками — см. §3).
3. **Критика пользователем принята:** ценность скана сейчас
   диагностическая (объяснить «почему X не работает»), НЕ самоулучшение.
   Решение: кнопка остаётся, но фича не рекламируется; список IP
   протухает (см. §4) — перед всерьёзным использованием обновлять из
   репо dpi-detector через jsDelivr.

## 3. Чистый замер на Т2 СПб (мобильный, 2026-09-06 вечер)

| Время | Инструмент | Условия | Результат |
|---|---|---|---|
| 26.08 | dpi-detector (скрин другого человека) | его сеть/условия | 97/110 OK (88%) |
| ~21:15 | наш CDN A/B (curl, домены) | ipset ON → OFF | «чинит 2, ломает 13» |
| ~21:45 | наш asn_scan (curl, чистый путь) | защита остановлена | 84/110 OK, 26 SYN DROP |
| ~22:05 | raw socket connect ×3 раунды | защита ВКЛ | «SYN DROP»-IP отвечают OK 3/3 |
| ~22:20 | dpi-detector 4.2.4 (`-t 34`) | winws2 hostlist (прозрачен) | **18/110 OK (16%)** |
| ~22:35 | наш asn_scan (curl, чистый путь) | защита остановлена | 66/110 OK, 44 SYN DROP |

**Выводы (важно!):**
- Один вечер — четыре разные картины. ТСПУ/транзит Т2 к этим ASN
  флапает по десяткам минут; одиночный скан = снимок, не приговор.
- Наши короткие curl-POST «проскальзывают» чаще (66–84 OK), их длинные
  keep-alive потоки режутся раньше (18 OK) — разные инструменты меряют
  разную экспозицию. Прямое сравнение цифр между инструментами
  некорректно; сравнивать надо динамику одного инструмента.
- SYN DROP-набор стабильно совпадает с ipset-«ломает»-набором при
  включённом ipset (п.1) — наш десинк реально ломает белый SNI-хендшейк
  к этим диапазонам (curl виснет в SSL_connect, а НЕ на SYN: проверено
  `curl -v` — «Trying IP...» затем schannel/ALPN и только потом таймаут;
  raw socket connect к тем же IP проходит мгновенно). Ярлык «SYN DROP»
  в нашем скане для этих случаев условен — это «TLS-фаза, сервер/ДПИ
  молчит». Не тратить время на «оживление» этих IP списком.
- CDN77 (169.150.x, 37.19.202.x) — флапает часами: OK → DROP → OK.
  Домен cdn.eso.org при этом резолвится в живые IP (в A/B «чинит»).

## 4. Что внедрено из dpi-detector

- **Список проб**: `lists/cdn-asn-probes.json` (110 × {id, asn, provider,
  ip, port}) — из их репо. **Протухает**: IP, опубликованные на GitHub,
  нулятся/фильтруются хостерами за недели (26 SYN DROP на чистом пути —
  в основном это). Обновление: jsDelivr
  `cdn.jsdelivr.net/gh/Runnin4ik/dpi-detector@main/tcp16.json`.
- **Белый SNI**: `lists/whitelist-sni.txt` (188 SNI, vk/2gis/alfabank/
  mail.ru экосистема). Используется нашим asn_scan (SNI=hcaptcha.com —
  устоявшийся компромисс; их дефолт example.com).
- **Блобы**: сгенерированы из whitelist через `tools/make_blob.py`
  (mail_ru, 2gis_ru, alfabank_ru, web_max_ru, hcaptcha_com + ранее
  chat_wb_ru; vk_com/max_ru — реальные захваты). Все в селекторе
  «Блоб фейка» автоматически. max_ru внутри содержит SNI
  www.onetrust.com (артефакт захвата — имей в виду).
- **Классификация ошибок**: наша теперь покрывает их таксономию
  (OK/DETECTED с «обрыв на N КБ»/TCP RST/SYN DROP/TIMEOUT/TLS RST).
- **DNS-спуфинг чек** в диагностике (`_check_dns_spoof_servers`):
  эталон через DoH Google (dns.google/resolve), ответы серверов через
  nslookup UDP; Google/Cloudflare/Яндекс/Quad9 на Т2 — чисто (2026-09-06).

## 5. Как устроен их инструмент (для будущего сравнения)

- `config.yml`: TCP_BLOCK_MIN_KB=12 / MAX=36 (полоса детекта),
  FAT_DEFAULT_SNI=example.com, connect 8s / read 12s, MAX_CONCURRENT=50.
- `tcp16_scanner.py`: FAT-проба — chunked keep-alive (10×4KB), детект по
  первому чанку за порогом; классификация фаз (tcp_connect → tls_handshake
  → sending_data → reading_data) через httpx event hooks.
- `utils/error_classifier.py`: таксономия статусов + Windows errno
  (WSAECONNRESET и т.п.).
- Тест 4 их меню: **перебор белых SNI (188) для заблокированных ASN** —
  фаза 2 «Параллельный перебор SNI, топ-3» (на Т2 06.09: для AS396982
  белый SNI не найден — все заблокированы).
- Слабое место их списка: пиннед-IP протухают (наш чистый замер: 26 SYN
  DROP; их домен-резолв-подход из dpich живучее).

## 6. Статус и что дальше

- ASN-скан: кнопка «ASN-скан (110 IP)» на вкладке CDN. Работает, ничего
  не ломает (защита возвращается; сервис-осведомлённо). Ценность —
  диагностика/поддержка. НЕ влияет на стратегии (пока 0 DETECTED).
- CDN-скан (домены, A/B ipset) — главный инструмент, не трогать.
- Тоггл «Блоб фейка» + «Подобрать блоба» — реализовано (см. TRIALS).
- Discord Voice «UDP-длина» — реализовано.
- Если пользователь принесёт список хостов из другой программы —
  подключить как источник (сейчас НЕ реализовано: скан читает
  встроенный список; точка расширения — начало `asn_scan`/скан-батареи).
- Т2 мобильная: любой одиночный скан — снимок. Для выводов — 2+ прогона.

## 7. Состояние машины/защиты после всех экспериментов (06.09 поздний вечер)

- Служба zapret2: остановлена (пользователь сам); обход — ручной
  winws2, пресет default, **ipset_catchall=False** (пользователь:
  «99% времени без него»; вернул осознанно).
- WARP: выключен пользователем; связь с API ZCode работает напрямую.
- config: fake_blob='' (google), discord_voice_mode=off.
