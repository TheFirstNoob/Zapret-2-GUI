7832076 style(main): status line, action panel, conflict inline, Z1 details
 frontend/css/app.css | 20 +++++++++++++++++---
 1 file changed, 17 insertions(+), 3 deletions(-)
diff --git a/frontend/css/app.css b/frontend/css/app.css
index 1287880..01d9922 100644
--- a/frontend/css/app.css
+++ b/frontend/css/app.css
@@ -200,21 +200,21 @@ body {
 }
 
 @keyframes fadeIn {
   from { opacity: 0; transform: translateY(8px); }
   to { opacity: 1; transform: translateY(0); }
 }
 
 /* тФАтФА Main Page тФАтФА */
 .main-grid {
   display: grid;
-  grid-template-columns: 1fr 1fr;
+  grid-template-columns: 1fr;
   gap: 16px;
 }
 
 .main-card-wide {
   grid-column: 1 / -1;
 }
 
 .main-card {
   position: relative;
   transition: var(--transition);
@@ -269,30 +269,36 @@ body {
   font-size: 12px;
   display: flex;
   align-items: center;
   gap: 6px;
   color: var(--text-secondary);
   margin-top: 2px;
 }
 
 .status-dot {
   display: inline-block;
-  width: 8px;
-  height: 8px;
+  width: 10px;
+  height: 10px;
   border-radius: 50%;
+  flex-shrink: 0;
 }
 
 .status-dot-ok {
   background: var(--ok);
   box-shadow: 0 0 8px var(--ok-glow);
 }
 
+.status-dot-fail {
+  background: var(--err);
+  box-shadow: 0 0 8px rgba(255, 82, 82, 0.3);
+}
+
 .status-dot-off {
   background: var(--text-muted);
 }
 
 .main-card-body {
   margin-bottom: 16px;
 }
 
 .info-row {
   display: flex;
@@ -1701,27 +1707,35 @@ label {
 
 .z1-details,
 #z1Card {
   margin-top: 14px;
   padding-top: 12px;
   border-top: 1px solid var(--border);
 }
 
 .z1-details summary,
 #z1Card summary {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  padding: 4px 8px;
+  margin: -4px -8px;
+  border-radius: var(--radius-sm);
   cursor: pointer;
   list-style: none;
   user-select: none;
   font-size: 13px;
   font-weight: 600;
   color: var(--text-secondary);
+  transition: background var(--transition), color var(--transition);
 }
 
 .z1-details summary::-webkit-details-marker,
 #z1Card summary::-webkit-details-marker {
   display: none;
 }
 
 .z1-details summary:hover,
 #z1Card summary:hover {
+  background: var(--surface-hover);
   color: var(--text);
 }
