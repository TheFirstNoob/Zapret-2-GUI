d81ad5e style(a11y): focus-visible, reduced-motion, system fonts
diff --git a/frontend/css/app.css b/frontend/css/app.css
index f2b3684..757360e 100644
--- a/frontend/css/app.css
+++ b/frontend/css/app.css
@@ -39,21 +39,21 @@
   padding: 0;
   box-sizing: border-box;
 }
 
 html, body {
   height: 100%;
   overflow: hidden;
 }
 
 body {
-  font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
+  font-family: Bahnschrift, "Segoe UI Variable", "Segoe UI", Tahoma, sans-serif;
   background: radial-gradient(ellipse at 50% 0%, #151b2d 0%, var(--bg) 55%);
   color: var(--text);
   line-height: 1.5;
   -webkit-font-smoothing: antialiased;
 }
 
 /* тФАтФА Layout тФАтФА */
 .app {
   display: flex;
   height: 100vh;
@@ -318,21 +318,21 @@ body {
 
 .info-row .value {
   color: var(--text);
   font-weight: 500;
   max-width: 60%;
   text-align: right;
   word-break: break-all;
 }
 
 .info-row .value.mono {
-  font-family: "Cascadia Code", "Consolas", monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   font-size: 11px;
   color: var(--text-muted);
 }
 
 .main-card-actions {
   display: flex;
   gap: 8px;
   align-items: center;
 }
 
@@ -438,20 +438,25 @@ body {
 .toggle-switch input:checked + .toggle-slider {
   background: var(--accent-bg);
   border-color: var(--accent);
 }
 
 .toggle-switch input:checked + .toggle-slider::before {
   transform: translateX(20px);
   background: var(--accent);
 }
 
+.toggle-switch input:focus-visible + .toggle-slider {
+  outline: 2px solid var(--accent);
+  outline-offset: 2px;
+}
+
 /* Toggles overlay */
 .toggles-overlay {
   display: none;
   position: absolute;
   inset: 0;
   background: rgba(10, 14, 23, 0.75);
   backdrop-filter: blur(4px);
   border-radius: var(--radius);
   z-index: 5;
   align-items: center;
@@ -470,21 +475,21 @@ body {
   text-align: center;
   padding: 20px;
   font-size: 13px;
   color: var(--text-secondary);
 }
 
 /* Textarea list editor */
 .textarea-list {
   width: 100%;
   min-height: 200px;
-  font-family: "Cascadia Code", "Consolas", monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   font-size: 13px;
   resize: vertical;
   line-height: 1.6;
 }
 
 /* тФАтФА Typography & Helpers тФАтФА */
 h2 {
   font-size: 18px;
   font-weight: 700;
   margin-bottom: 16px;
@@ -575,21 +580,25 @@ p {
   gap: 8px;
   padding: 10px 18px;
   border-radius: var(--radius-sm);
   border: 1px solid transparent;
   font-size: 14px;
   font-weight: 600;
   cursor: pointer;
   transition: var(--transition);
   background: var(--surface-hover);
   color: var(--text);
-  outline: none;
+}
+
+.btn:focus-visible {
+  outline: 2px solid var(--accent);
+  outline-offset: 2px;
 }
 
 .btn:hover:not(:disabled) {
   transform: translateY(-1px);
   box-shadow: var(--shadow-sm);
 }
 
 .btn:disabled {
   opacity: 0.5;
   cursor: not-allowed;
@@ -658,21 +667,21 @@ p {
   border-color: var(--accent);
   box-shadow: 0 0 0 3px var(--accent-bg);
 }
 
 .input::placeholder, .textarea::placeholder {
   color: var(--text-muted);
 }
 
 .textarea {
   min-height: 200px;
-  font-family: "JetBrains Mono", "Fira Code", Consolas, monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   font-size: 13px;
   line-height: 1.6;
   resize: vertical;
 }
 
 label {
   display: block;
   font-size: 12px;
   color: var(--text-secondary);
   margin-bottom: 6px;
@@ -773,21 +782,21 @@ label {
 
 .detail-row:last-child {
   border-bottom: none;
 }
 
 .detail-label {
   color: var(--text-secondary);
 }
 
 .detail-value {
-  font-family: "JetBrains Mono", Consolas, monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   color: var(--text);
 }
 
 /* тФАтФА Profiles Page тФАтФА */
 .profile-grid {
   display: grid;
   grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
   gap: 14px;
 }
 
@@ -1075,21 +1084,21 @@ label {
 }
 
 .test-log {
   max-height: 220px;
   overflow-y: auto;
   margin-top: 14px;
   padding: 12px 14px;
   background: rgba(0, 0, 0, 0.35);
   border: 1px solid var(--border);
   border-radius: var(--radius-sm);
-  font-family: "JetBrains Mono", Consolas, "Cascadia Code", monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   font-size: 12px;
   color: var(--text-secondary);
   line-height: 1.7;
   display: none;
   scrollbar-width: thin;
   scrollbar-color: var(--border) transparent;
 }
 
 .test-log::-webkit-scrollbar { width: 6px; }
 .test-log::-webkit-scrollbar-track { background: transparent; }
@@ -1218,21 +1227,21 @@ label {
 }
 
 .badge-alias {
   background: var(--surface-hover);
   color: var(--text-tertiary);
   font-size: 10px;
   opacity: 0.7;
 }
 
 .time-ms {
-  font-family: "JetBrains Mono", Consolas, monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
   font-size: 12px;
 }
 
 .time-fast { color: var(--ok); }
 .time-ok { color: var(--text); }
 .time-slow { color: var(--warn); }
 .time-too-slow { color: var(--err); }
 
 /* тФАтФА Live Strategy Blocks тФАтФА */
 #testLiveBlocks {
@@ -1335,26 +1344,35 @@ label {
 
 .live-block.expanded table {
   display: table;
 }
 
 .live-block table thead th {
   cursor: default;
 }
 
 @media (prefers-reduced-motion: reduce) {
+  *, *::before, *::after {
+    animation-duration: 0.01ms !important;
+    animation-iteration-count: 1 !important;
+    transition-duration: 0.01ms !important;
+    scroll-behavior: auto !important;
+  }
   .live-block.active .live-block-lamp {
     animation: none;
   }
   .live-block-arrow {
     transition: none;
   }
+  .progress-bar .fill::after {
+    display: none;
+  }
 }
 
 /* тФАтФА Result Grid / Tiles тФАтФА */
 .result-grid {
   display: grid;
   grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
   gap: 12px;
   margin-top: 12px;
 }
 
@@ -1387,21 +1405,21 @@ label {
 }
 
 .result-tile .tile-status {
   font-size: 11px;
   font-weight: 700;
   text-transform: uppercase;
 }
 
 .result-tile .tile-time {
   font-size: 11px;
-  font-family: "JetBrains Mono", Consolas, monospace;
+  font-family: "Cascadia Mono", Consolas, monospace;
 }
 
 .result-tile .tile-badges {
   display: flex;
   gap: 3px;
   flex-wrap: wrap;
   justify-content: center;
 }
 
 .result-tile .tile-badge {
@@ -1576,21 +1594,21 @@ label {
   gap: 8px;
 }
 
 .blocked-chip {
   background: rgba(255, 82, 82, 0.12);
   border: 1px solid rgba(255, 82, 82, 0.3);
   color: var(--err);
   padding: 4px 10px;
   border-radius: 999px;
   font-size: 12px;
-  font-family: var(--font-mono, monospace);
+  font-family: var(--font-mono, "Cascadia Mono", Consolas, monospace);
 }
 
 /* тФАтФА Ring Gauge тФАтФА */
 .ring-gauge-wrap {
   display: flex;
   justify-content: center;
   margin-bottom: 16px;
 }
 
 .ring-gauge {
@@ -1903,10 +1921,16 @@ label {
 .z1-details summary::-webkit-details-marker,
 #z1Card summary::-webkit-details-marker {
   display: none;
 }
 
 .z1-details summary:hover,
 #z1Card summary:hover {
   background: var(--surface-hover);
   color: var(--text);
 }
+
+.z1-details summary:focus-visible,
+#z1Card summary:focus-visible {
+  outline: 2px solid var(--accent);
+  outline-offset: 2px;
+}
